# Laboratorio de Criptoanálisis del Cifrado de Vigenère

Este repositorio contiene un laboratorio implementado en **Jupyter Notebook**
para el cifrado, descifrado y criptoanálisis del cifrado de **Vigenère**.

El laboratorio incluye:
- Implementación del cifrado y descifrado de Vigenère
- Análisis estadístico del texto cifrado
- Recuperación automática de la clave
- Descifrado del mensaje original

---

## Contenido del repositorio

- `Vigenere_Laboratorio.ipynb`  
  Notebook principal con toda la implementación y explicación del laboratorio.

- `prueba.txt`  
  Archivo de texto que contiene el mensaje cifrado a analizar.

- `environment.yml` (opcional)  
  Archivo para recrear el entorno de ejecución con Anaconda.

---

## 🛠️ Requisitos

- Anaconda o Miniconda instalado
- Python 3.9 o superior
- Jupyter Lab o Jupyter Notebook

---

## Creación del entorno (recomendado)

Desde la raíz del repositorio, ejecutar:

```bash
conda env create -f environment.yml
```
## Ejecucion del entorno

1. Activar el entorno con Anaconda

```bash
conda activate vigenere-lab
```
2. Iniciar Jupyter Notebook

```bash
jupyter notebook
```
3. Abrir el archivo vigenere_lab.ipynb

4. Ejecutar las celdas en orden de arriba hacia abajo
